---
const { name, alt, width, height, type } = Astro.props;
const dir = type === "icon" ? "icons" : "logos";
---

<svg role="img" title={alt} aria-label={alt} width={width} height={height}>
    <use href={`/Astrofolio/${dir}/${name}.svg#${name}`}></use>
</svg>
