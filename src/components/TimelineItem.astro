---
import SVGComponent from "./SVGComponent.astro";

const { experience } = Astro.props;
---
<div class="timeline__item__container">
    <div class="timeline__item__logo">
        {experience.logo ? <SVGComponent width={32} height={32} name={experience.logo} type="logo" /> : <SVGComponent width={32} height={32} name="company" type="logo" />}
    </div>
    <div class="timeline__item__content">
        <h3>{experience.role}</h3>
        <div class="timeline__item__company">
            <h5>{experience.company} - {experience.location}</h5>
            <p class="timeline__item__date">{new Date(experience.startDate).toLocaleDateString("en-US", {month: "long", year: "numeric"})} - {experience.endDate === "Current" ? "Current" : new Date(experience.endDate).toLocaleDateString("en-US", {month: "long", year: "numeric"})}</p>
        </div>
        <p class="timeline__item__description">{experience.description}</p>
    </div>
</div>

<style>
    .timeline__item__container {
        display: inline-flex;
        gap: 1.5rem;
        padding: 1rem 0;
        width: 100%;

        &::after {
            content: "";
            position: absolute;
            top: 21%;
            bottom: 21%;
            left: 1.9rem;
            width: 3px;
            height: -webkit-fill-available;
            background-color: #aae;
            z-index: -1;

            @media (width <= 512px) {
                content: none;
            }
        }

        @media (width <= 512px) {
            flex-direction: column;
            gap: 1rem;
            align-items: center;
        }
    }

    .timeline__item__logo {
        width: auto;
        display: flex;
        justify-content: center;
        align-items: flex-start;
        flex: 1;
        height: fit-content;
        background-color: #aae;
        border-radius: 100%;
        padding: 1rem;
        z-index: 1;
    }

    .timeline__item__content {
        width: 100%;
        display: flex;
        flex-direction: column;
        gap: 0.5rem;
        align-items: flex-start;
        flex: auto;
    }

    .timeline__item__company {
        display: flex;
        gap: 2rem;
        align-items: center;

        @media (width <= 512px) {
            flex-direction: column;
            gap: 0.5rem;
            align-items: flex-start;
            padding: 1rem 0;
        }
    }

    .timeline__item__date {
        margin: 0;
        background-color: #aae;
        padding: 0.25rem 0.5rem;
        color: var(--color-light-fg);
        border-radius: 1rem;
        font-size: 0.85rem;
        font-weight: 500;
    }

    .timeline__item__description {
        padding: 1rem 0;
    }
}
</style>
